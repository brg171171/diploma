---
all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/netology_diploma

  children:
    bastion:
      hosts: {}

    prometheus:
      hosts: {}

    grafana:
      hosts: {}

    elasticsearch:
      hosts: {}

    kibana:
      hosts: {}
